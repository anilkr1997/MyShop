<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vivid.myshop">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".userprofile.userprofile"></activity>
        <activity android:name=".bucketcard.cardview" />
        <activity android:name=".checkout.checkout" />
        <activity android:name=".customerdetial.coustomerregistation" />
        <activity android:name=".customerdetial.costumerlogin" />
        <activity android:name=".fullcontantview.fullitemview" />
        <activity android:name=".MainActivity" />
        <activity android:name=".splaceschreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>